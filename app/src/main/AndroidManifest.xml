<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mantis.project.mantisbt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".controller.VolleySingleton"
        android:allowBackup="true"
        android:icon="@mipmap/ic_mantis"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mantis"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"></activity>
        <activity android:name=".Detail_issue" ></activity>
        <activity android:name=".Addnote"></activity>
    </application>

</manifest>